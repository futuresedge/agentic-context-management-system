services:
  typedb:
    image: vaticle/typedb:latest
    container_name: nexus-typedb
    ports:
      - "1729:1729"
    volumes:
      - typedb-data:/opt/typedb-all/server/data
    restart: unless-stopped

volumes:
  typedb-data:
